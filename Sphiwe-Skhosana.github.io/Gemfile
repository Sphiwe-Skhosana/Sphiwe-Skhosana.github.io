# Only needed to preview the site on your own computer (see README). GitHub ignores it.
source "https://rubygems.org"
gem "github-pages", group: :jekyll_plugins
gem "webrick"

# Windows needs time-zone data for Jekyll
gem "tzinfo-data", platforms: [:mingw, :x64_mingw, :mswin, :jruby]
